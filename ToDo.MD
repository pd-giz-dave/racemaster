# ToDo

## Add test suite

- [ ] JS logic
- [ ] view logic?

## Add importing results from other sources

- [ ] BLE SYNC receiver: either a Web-Bluetooth page in the racemaster web app (Chrome/Edge
      only — browsers can't act as a BLE peripheral, so the phone would have to be the
      peripheral and the browser the central, with an unavoidable manual "Connect" click) or a
      separate small receiver app writing a file for later import (racemaster's own `ToDo.MD`
      already lists CSV import as planned separately, so this could piggyback on that)
- [ ] Racemaster-mobile Android app that logs times and numbers (replacing the clock and finish sheets)
- [ ] provide a page that shows mobile results available and allow cherry pick to create results from,
  
- [ ] CSV file - make SI Results a generic CSV import

## Public datasets

Public datasets are read-only for non-owners and non-admins.
Allow read-only access to public datasets even if not logged in?

## Add a notes field to event setup

It appears as a paragraph before the results in the published html

