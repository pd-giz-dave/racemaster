# ToDo

- Add column sorts to lists where appropriate
- Add test suite

## Add splits option to results when using dibbers

How? 
SI Timing only reports control code numbers, so a naming scheme is required.
SI Timing system provides: NumSplits, [ControlCode, Split] * NumSplits, 
split is  elapsed time from the previous control.

## Mimic the SI Timing system HTML results

- see eg in nextcloud/races/2026/lmv/index.html
- references their CSS and JS

## Combine male/female categories

The only difference in the tables is the category name.
So have two cat name columns, one for male and one for female, in a single table.
