'use strict';

// Canonical domain value constants — not user-visible text (see strings.js's own doc for that
// distinction), just the fixed set of values gender/course fields are actually compared against
// throughout the app.

export const GENDER = {
  FEMALE: 'Female',
  MALE:   'Male',
};

export const COURSE = {
  JUNIORS: 'Juniors',
  SENIORS: 'Seniors',
};
